<!DOCTYPE html>

<html>

    <head>
        <title>Rango</title>
    </head>

    <body>
        <h1>Rango says... hello!</h1>
        
        <p>
            <strong>Most favourite categories:</strong><br/>
            {%if categories %}
                <ul>
                    {% for category in categories %}
                    <li><a href="/rango/category/{{category.slug}}">{{ category.name }}</a></li>
                    {% endfor %}
                </ul>
            {% else %}
                <strong>There are no categories to present.</strong><br/>
            {% endif %}
            
            <a href="/rango/add_category/">Add a New Category</a>
        </p>
        
        <p>
            <strong>Top 5 most viewed pages:</strong><br/>
            {%if pages %}
                <ul>
                    {% for page in pages %}
                    <li><a href="{{ page.url }}">{{ page.title }}</a></li>
                    {% endfor %}
                </ul>
            {% else %}
                There are no pages to display.<br/>
            {% endif %}
        </p>
        <a href="/rango/about/">About</a>
    </body>
</html>